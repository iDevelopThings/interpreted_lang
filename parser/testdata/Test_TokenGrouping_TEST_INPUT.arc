import "...";
import "..."

object User {
  name string
  age  int
}

func (u User) NoReceiverFunc() {
  var user []User = {
	  User{name:"John"},
	  User{name:"Bruce"},
  };

  if 1 == 2 {
	return 0;
  } else {
	return 0;
  }

  fmt::printf("..", user.b);
  i++;
}

func test() string {
  return "...";
}