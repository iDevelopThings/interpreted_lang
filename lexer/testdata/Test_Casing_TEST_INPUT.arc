FUNC OBJECT IMPORT ENUM VAR RETURN BREAK CONTINUE IF ELSE FOR AS STEP OR DEFER NONE TRUE FALSE HTTP GET POST PUT HEAD
func object import enum var return break continue if else for as step or defer none true false http get post put head
Func Object Import Enum Var Return Break Continue If Else For As Step Or Defer None True False Http Get Post Put Head
